# OS junk
.DS_Store
Thumbs.db

# Local tooling (not used by this project, but harmless to ignore)
node_modules/
.vscode/
.idea/

_site/

# Local scan output (the workflow builds it inside the Pages artifact; it is
# intentionally not committed to main)
docs/news-report.json
